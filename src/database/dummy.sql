create table course(
cl_crseno int,
cl_subjcode varchar(10),
cl_title varchar(50),
cl_credithours int,
cl_description varchar(500),
)

create table curriculum(
    cl_crseno int,
    deg_level varchar(20),
    curi_area 
)
